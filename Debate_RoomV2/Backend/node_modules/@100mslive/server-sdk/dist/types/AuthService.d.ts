export declare class AuthService {
    private accessKey;
    private secret;
    constructor(accessKey: string, secret: string);
    getManagementToken({ issuedAt, notValidBefore, validForSeconds }?: CreateTokenConfig): Promise<Token>;
}
export interface CreateTokenConfig {
    /**
     * value in seconds
     */
    issuedAt?: number;
    /**
     * value in seconds
     */
    notValidBefore?: number;
    /**
     * value in seconds, once the token is valid how long will it stay valid
     */
    validForSeconds: number;
}
export interface Token {
    token: string;
}
